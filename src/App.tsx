import React from 'react';

import { globalAgent } from 'http';
import GlobalStyle from './styles/global';

const App: React.FC = () => (
  <>
    <h1>Hello!</h1>
    <GlobalStyle />
  </>
);

export default App;
